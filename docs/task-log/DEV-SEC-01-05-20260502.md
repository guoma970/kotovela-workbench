# DEV-SEC-01..05 Security & Environment Pass

- date: 2026-05-02
- branch: feature/snapshot-sync-ready
- scope: Kotovela Workbench only

## Changes

- DEV-SEC-01: `vite.config.ts` now resolves `PROJECT_ROOT` from `process.env.PROJECT_ROOT`, with safe fallback to `OPENCLAW_PROJECT_ROOT` and then `process.cwd()` so local runs keep working.
- DEV-SEC-02: `.gitignore` already blocks local env secrets via `*.local` and `.env*.local`, including `.env.local` / OIDC-token style local overrides.
- DEV-SEC-03: API handlers checked: `api/office-instances.ts` and `api/model-usage.ts` both reject non-GET methods with `405` and `Allow: GET`.
- DEV-SEC-04: `api/office-instances.ts` keeps upstream SSRF controls: valid URL parse, `https:` only, rejects localhost/private/internal hosts, and supports `OFFICE_INSTANCES_UPSTREAM_ALLOW_HOSTS` allowlist.
- DEV-SEC-05: Added direct `zod` dependency and strict schemas for external office-instance payloads:
  - upstream API payload validation in `api/office-instances.ts`
  - local snapshot payload validation in `server/officeInstances.ts`

## Verification

- `/Users/ztl/.openclaw/bin/kotovela-hub-verify lint`: pass (`eslint .`, exit 0)
- `/Users/ztl/.openclaw/bin/kotovela-hub-verify build`: pass (`tsc -b && vite build`, exit 0; output asset `dist/assets/index-DV1Qweob.js`)
- `/Users/ztl/.openclaw/bin/kotovela-hub-verify test`: pass (`node scripts/run-stabilization-suite.mjs`, 32/32 pass; run_id `stab-1777688030500`)
- `/Users/ztl/.openclaw/bin/kotovela-hub-verify build-internal`: pass (`tsc -b && vite build --mode internal`, exit 0; output asset `dist/assets/index-xUJAag5x.js`)
- `/Users/ztl/.openclaw/bin/kotovela-hub-verify build-opensource`: pass (`tsc -b && vite build --mode opensource`, exit 0; output asset `dist/assets/index-CHaKh0DN.js`)
- `/Users/ztl/.openclaw/bin/kotovela-hub-verify capture-web`: pass; summary `.evidence/dev20260501-ui01/summary.json`
- post-capture `/Users/ztl/.openclaw/bin/kotovela-hub-verify lint`: pass (`eslint .`, exit 0)

## Screenshot Evidence

- dark dashboard: `screenshots/DEV-SEC-01-05-dashboard-dark.png`
- light dashboard: `screenshots/DEV-SEC-01-05-dashboard-light.png`
- current/internal dashboard capture: `screenshots/DEV-20260501-UI-01-dashboard.png`
- additional current captures: `screenshots/DEV-20260501-UI-01-agents.png`, `screenshots/DEV-20260501-UI-01-system-settings.png`, `screenshots/DEV-20260501-UI-01-evidence-validation.png`, `screenshots/DEV-20260501-UI-01-sidebar.png`

## Risk / Decision Notes

- No new backend or large module was introduced.
- Strict zod schemas are intentionally applied at external payload boundaries; malformed upstream/snapshot data now fails closed and falls back through existing error paths.
- Dedicated opensource preview screenshot remains a wrapper limitation: `capture-web` starts the project capture flow and does not expose a `--mode opensource` / preview-mode selector. Build:opensource was verified successfully.
- Existing workspace had unrelated modified/untracked files before this task; commit must include only this task's paths or wait for owner cleanup.
